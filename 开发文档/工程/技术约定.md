# 技术约定

本文件记录工程任务需要读取的技术约束。当前项目已确认主要游戏工程使用 Godot；未确认的工程细节仍不得由 AI 擅自写死。

## 当前技术状态

- 已确认主要载体为 Godot 4.5 stable。
- Godot 工程位置：`游戏工程/`。
- Godot 项目文件：`游戏工程/project.godot`。
- `project.godot` 当前特征包含 `4.5` 与 `Mobile`。
- 当前渲染方式：`mobile`。
- 已确认 Godot 脚本语言使用 GDScript。
- 未确认辅助前端框架或外部工具栈。
- 未确认包管理器。
- 未确认构建命令。
- 未确认测试命令。
- `游戏工程/` 已有 V0.1 基础验证原型，不再是空项目。
- 当前主场景：`游戏工程/scenes/v01/V01Game.tscn`。
- 当前 V0.1 主脚本：`游戏工程/scripts/v01/v01_game.gd`。
- 当前 V0.1 卡牌数据：`游戏工程/scripts/v01/card_catalog.gd`。
- 当前 AI 单位美术正式透明 PNG 目录：`游戏工程/assets/units/`。
- 当前 AI 单位美术源图目录：`游戏工程/assets/units/source_sheets/`。
- 当前 AI 单位美术后处理脚本：`tools/ai_unit_art/process_unit_sheet.py`。
- 当前 AI 单位美术流程文档：`开发文档/美术/AI单位美术生产流程.md`。
- 已确认主要目标用户为手机竖屏用户；工程和 UI 实现应优先考虑竖屏移动端体验。
- 已确认最终产品方向需要联机对战；V0.1/V0.2 的本地玩家对脚本 Bot 只是早期验证方式。
- 暂未确认项目级 Godot CLI 路径或跨协作者统一验证命令。
- 本机临时验证记录：当前设备可通过 `C:\Tools\godot\godot.cmd` 调用 Steam 版 Godot，实际版本为 `4.5.2.stable.steam`；这不是项目标准配置，其他协作者不应假设存在该路径。
- 当前 Codex 进程尚未刷新 Windows PATH，因此直接运行 `godot` 可能仍不可用；重启 Codex/终端后应再验证 `godot --version`。

## 当前 V0.1/V0.2 工程结构（已模块化）

V0.1/V0.2 原型已从单文件拆分为按职责分层的模块结构。以下是当前实现事实；后续可随 Godot 编辑器整理继续调整。

### 目录与职责

- `scenes/v01/V01Game.tscn`：V0.1 主场景，根节点为 `Control`，挂载 `scripts/v01/v01_game.gd`。
- `scripts/v01/v01_game.gd`：对局控制器（Control 节点入口）。仅负责生命周期、输入分发、每帧驱动模拟器与绘制器、屏幕状态切换；不再直接包含战斗逻辑或绘制实现。V0.3 新增调试快捷键（+/−加费、T强制完成任务、R重开、D切换Bot牌组、S重置种子、F调试面板开关）。
- `scripts/v01/card_catalog.gd`：8 张 trial 原型卡及任务/进化字段（数据层，未改动）。
- `scripts/config/game_config.gd`：集中存放平衡常量、地图尺寸、Bot 牌组与美术路径；各脚本以 `const Config = preload(...)` 引用，沿用 preload 风格，不使用 class_name。
- `scripts/simulation/battle_simulator.gd`：战斗模拟核心。纯逻辑，持有对局运行时状态（时间、费用、单位、基地、法术特效、统计、事件日志、进化闪光），驱动单位移动/碰撞分离/索敌/攻击、法术命中、光环、清屏与胜负结算；通过 `running` 标记对局是否进行中；出牌统一经 `try_play_card` 入口。
- `scripts/simulation/task_system.gd`：任务与进化系统。追踪局内任务进度、卡牌使用次数、完成时间戳；任务完成后自动进化卡牌，并解析当前生效卡牌定义（基础/进化）；提供 `force_complete_task` 调试接口与 `task_progress_ratio` 进度条接口。
- `scripts/simulation/bot_brain.gd`：Bot AI。支持随机从卡池选 6 张或使用固定牌组；与玩家共用 `BattleSimulator.try_play_card` 入口；提供 `set_use_random_deck` 切换与 `current_deck` 查询接口。
- `scripts/presentation/ui_painter.gd`：屏幕绘制与布局。负责选卡/战场/结算界面绘制及屏幕↔逻辑坐标换算，持有布局矩形缓存供控制器做点击命中测试。
- `scripts/presentation/canvas_helpers.gd`：低层绘制原语（面板、文本、贴图）与单位美术加载/缓存/几何占位回退。
- `scripts/support/map_math.gd`：纯地图与几何工具（阵营换算、基地/桥位置、部署区裁剪、点到线段距离），无运行时状态。
- `assets/units/`：AI 单位美术透明 PNG；文件名为 `<art_id>_front.png` 与 `<art_id>_back.png`。
- `assets/units/source_sheets/`：AI 单位美术左右正背面源图；不应由游戏运行时直接引用。
- `project.godot`：已设置 `run/main_scene` 指向 V0.1 主场景，并设置 720x1280 竖屏优先视口。

### 模块间接口约定

- 控制器每帧顺序：`simulator.advance_time` → `task_system.check_mana`（双方）→ `bot_brain.update` → `simulator.update_units` → `simulator.update_spell_effects` → 检测 `simulator.running` 切换结算界面。顺序与拆分前一致。
- 出牌统一经 `BattleSimulator.try_play_card(side, card, position)`，`card` 为 `TaskSystem.active_card_by_id` 解析后的生效卡牌。
- 任务/进化事件通过 `TaskSystem` 回调 `BattleSimulator.record_task_completed` 与 `push_event` 回写统计与日志；`BattleSimulator` 在伤害/出牌时回调 `TaskSystem.track_*`。
- 模拟器与 UI 解耦：模拟器使用逻辑坐标与 `running` 标记，不依赖 `screen_mode`；绘制器读取模拟器状态但不修改。

### 当前调参实现

- `MANA_PER_SECOND = 0.5`；生物单位生成时使用 `UNIT_RADIUS_SCALE = 2.0` 放大卡牌数据中的单位半径，法术范围和基地半径不随该倍率变化。
- 本机已通过 Godot 4.5.2 headless 主场景加载检查（退出码 0），并额外跑过临时冒烟脚本自动对战 1500 帧验证模拟链路无回归（单位/战斗/任务/进化/统计正常），脚本验证后已删除。

## 本机验证记录（非项目标准）

- 本机版本检查：`C:\Tools\godot\godot.cmd --version`。
- 本机 V0.1 主场景短跑加载检查：`C:\Tools\godot\godot.cmd --headless --path "游戏工程" --log-file ".godot/godot-cli.log" --quit-after 5`。
- 2026-08-12 复测：确认没有手动打开的 Godot 进程后，上述版本检查与 headless 短跑加载检查均通过，未复现应用程序错误弹窗，检查后也没有残留 Godot 进程。
- 上述命令只记录当前设备的可用路径，不代表协作者必须安装 Steam 版或使用 `C:\Tools\godot\godot.cmd`。
- 协作者只需使用 Godot 4.5 stable，并确保自己的命令行能调用对应可执行文件；具体路径可因设备而异。
- Steam 版 Godot 在当前 Codex 会话里直接运行 `godot` 仍可能受 PATH 刷新影响；若要使用裸命令，重启 Codex/终端后再检查 `godot --version`。
- 不带 `--log-file` 时，当前环境曾因默认 `user://logs/...` 无法打开而崩溃；随后还出现过 `godot.windows.opt.tools.64.exe` 应用程序错误弹窗。命令行验证应保留项目内日志路径。
- 当前命令会输出 `Failed to read the root certificate store`，但退出码为 0，暂未发现会影响本地原型加载。
- 若 Steam 版 Godot CLI 后续再次出现应用程序错误弹窗，可优先使用官方独立 Standard 版 Godot 4.5 stable 作为命令行验证入口，Steam 版仍可用于编辑器内手动验收。

## 技术栈确认规则

在没有人类设计师确认、也没有既有工程文件证明的情况下，AI 不得擅自写死尚未确认的技术细节。当前确认 Godot 4.5 stable 与 mobile 渲染方向，不代表目录结构、插件、导出目标、构建流程或测试流程已经确认。

当更多技术栈细节确认后，应补充：

- GDScript 版本与语言使用细则。
- 插件或第三方依赖。
- 目录结构约定。
- 安装命令。
- 本地启动命令。
- 构建命令。
- 测试命令。
- 移动端导出目标与导出配置。

## 坐标、速度与原型表现约定

- 对局地图的长边应作为 100 个逻辑单位处理，避免不同设备尺寸导致单位表现不同。
- 速度数值的单位是逻辑单位/秒；速度 1 表示每秒移动 1 个逻辑单位，速度 8 表示每秒移动 8 个逻辑单位。
- 实现时应优先让战斗逻辑使用逻辑单位，再按当前视口尺寸换算到屏幕像素。
- 不应把设备像素值作为权威移动速度或战斗距离。
- V0.1 中单位不需要美术资源；每个生物可用 Godot 绘制的简单几何图形（圆、三角、方）加一个字表示。
- 当前 AI 单位美术属于版本线外表现层实验：`v01_game.gd` 会按单位 `art_id` 读取 `assets/units/<art_id>_front.png` 或 `<art_id>_back.png`，并通过 `Image.load_from_file()` 创建运行时纹理；缺图时回退到 V0.1 几何占位表现。
- 当前 AI 单位美术实现不依赖提交 `.png.import` 文件；若 Godot 编辑器后续生成导入缓存，不应改变正式资源命名和 fallback 规则。
- AI 单位图片不参与碰撞、索敌、攻击距离、费用、任务或进化判定；这些逻辑仍使用 `unit["radius"]`、卡牌数值和任务状态。
- 卡牌数据文件 `card_catalog.gd` 不写入美术路径；基础形态使用卡牌 `id`，进化形态使用运行时 `evolved_id` 作为表现层 `art_id`。
- 蓝/红阵营识别环由 `v01_game.gd` 单独绘制，不烘焙进单位图片。

## 设计师文档维护约定

- V0.2 需要创建并维护 `开发文档/设计/设计师文档.md`，用于让设计师阅读和编辑卡牌数值说明。
- 设计师文档是说明文档，可以当成“有注释的 JSON 数据”；设计师有阅读代码的能力，但需要完善注释帮助定位可编辑内容。
- 卡牌速度、血量、攻击力等关键数值发生变化时，应同步检查设计师文档。
- 如果发现设计师文档变化了，应向设计师询问是否需要同步到工程卡牌数据或其他文档。
- 在项目确认自动同步或单一数据源之前，不得假设设计师文档或代码单方面具有最终权威；若二者冲突，先向人类确认。
- V0.2 任务与进化基础边界已确认：8 张初版卡都有任务和进化；每张卡默认 1 个任务；暂时不做局外任务配置；先做 1 层进化；任务进度仅在局内生效；任务完成后自动且不可避地进化到对应下一形态。
- 8 张卡各自的具体任务条件和进化效果已由项目负责人确认，并已记录到 `开发文档/设计/已确认玩法基础.md` 与 `开发文档/设计/设计师文档.md`。
- 当前 V0.2 原型实现仍沿用 V0.1 工程入口：任务/进化数据集中在 `游戏工程/scripts/v01/card_catalog.gd`，局内任务追踪、自动进化、进化后单位/法术行为和 UI 展示集中在 `游戏工程/scripts/v01/v01_game.gd`。
- 任务完成后，卡牌后续使用会读取进化后的临时卡牌定义；已在场上的单位保留生成时属性，不会随卡牌进化反向改变。
- 当前 V0.2 数值微调：裂魂矢对基地伤害为 33；星屑漂流数量为 6；投火手攻击落点 AOE 为固定半径 2，不再采用增量 AOE 规则。
- 2026-08-12 本机 V0.2 验证：临时 GDScript 冒烟脚本直接触发 8 张卡任务并检查关键进化数值，通过后已删除临时脚本；随后主场景 headless 加载检查通过，命令仍为 `C:\Tools\godot\godot.cmd --headless --path "游戏工程" --log-file ".godot/godot-cli.log" --quit-after 5`。

## 联机实现约束

- 奥术前线已确认最终需要联机对战；后续工程整理应避免继续把战斗逻辑、玩家输入、Bot 输入、UI 展示和本地状态强耦合在同一个不可拆脚本中。
- 已确认 V0.3 不直接实现联网，但应记录当前本地原型中会影响联网的假设，例如直接读写全局对局状态、随机数来源、输入命令格式、单位生成顺序和结算事件来源。
- 已确认 V0.4 作为联机前工程准备版本，优先分离战斗模拟、输入命令、事件日志、卡牌数据和 UI 表现，为最小联机对局打基础。
- 已确认 V0.5 做最小联机对局原型，目标是验证两端连接、同一房间、双方出牌输入、基础状态同步和断线处理，不包含正式匹配、账号、排行榜或商业化系统。
- 联机架构尚未确认：需要人类决定服务端权威、房主权威或确定性同步等方案；在确认前，AI 不应擅自引入联网插件、外部后端服务或账号系统。

## 临时实现原则

- 原型应小、清晰、可检查、易替换。
- 若实现游戏工程内容，应优先围绕 Godot 组织。
- 不要直接改动不理解的 `project.godot` 配置项；优先保持 Godot 编辑器生成的结构。
- 若只是辅助说明、数据验证或轻量外部工具，仍可在人类同意或任务需要时使用更轻量的脚本/网页原型。
- 卡牌、敌人、Buff、经济、遭遇、成长等规则优先使用结构化数据表达。
- 游戏设计数据应与渲染/UI 代码分离。
- 不引入大型依赖、游戏引擎、构建系统或外部服务，除非人类确认或现有工程已经采用。

## 代码风格占位约定

- 函数应围绕游戏领域意图命名。
- 核心对象应有清晰类型，例如卡牌、令牌、Buff、敌人、关卡、奖励、玩家状态。
- 战斗逻辑、经济规则、存档、随机生成不要依赖隐式全局状态。
- 随机行为应尽量可测试，例如使用种子 RNG 或可注入随机源。
- 平衡数值优先放在数据或配置中，除非它们属于基础公式。
- 对确定性规则、存档兼容、经济计算、战斗边界情况添加测试。
