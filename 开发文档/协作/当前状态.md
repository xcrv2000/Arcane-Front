# 当前状态

最后更新：2026-08-14（见习骑士征召禁用与图鉴限制标记）

本文件是高频状态页。每次非平凡 AI 任务都应读取它；任务结束后，如果进度、风险、开放问题或活跃假设变化，也应更新它。

## 当前事实

- 项目名称：奥术前线。
- 项目性质：多人类设计师与 AI 协作推进的独立游戏项目。
- 已建立较大更新摘要流程；协作者每次开始工作时应检查 `开发文档/协作/更新摘要.md` 的最新未读条目。
- 已确认协作记录需要区分创建者与协作者：更新摘要应写明创建者，阅读标记应写明协作者，决策记录新条目应写明创建者。
- 已确认协作者签名是本地/会话信息，不是仓库共享事实；共享文档不得写死某个签名作为“本机签名”，AI 不知道当前操作者签名时应先询问。
- 当前阶段：V0.1 与 V0.2 均已完成并通过人工验收；V0.3 已完成 P1/P2/P3 与文档，待人工验收；V0.4 已确认为下一版本（联机对局），计划草案已写入 `plan/V0.4联机对局开发计划.md`；已确认最终产品需要联机对战。
- 已确认主要载体为 Godot 4.5 stable，主要目标用户为手机竖屏。
- 已确认 Godot 脚本语言使用 GDScript。
- `游戏工程/` 中已创建 Godot 空项目，项目文件为 `游戏工程/project.godot`。
- 当前 Godot 项目特征包含 `4.5` 与 `Mobile`，渲染方式为 `mobile`。
- 已确认基础类型参考为类《皇室战争》/《随从大师》的即时战术对战游戏。
- 已确认奥术前线最终需要实现联机对战；V0.1/V0.2 的本地玩家对脚本 Bot 只是早期验证方式。
- 初版卡池为 8 种；当前权威主卡目录已扩充为 16 张，其中 15 张可携带、见习骑士征召 1 张因强度过高禁用，另有 3 个不可携带的百骸公国侍童衍生单位；单局仍始终在开局前配置 6 种卡进入对局。
- 已确认双方各有一个基地，每个基地 300 血。
- 已确认 V0.1 可以先做本地玩家对脚本 Bot，不做联网对战。
- 已确认 V0.1 只允许通过摧毁基地结束对局；地图中央、双桥中间的河位置放置开发观察用时钟。
- 已确认 V0.1 不需要做完任务和进化系统，但应预留接口与 UI 位置。
- 已确认 V0.1 单位不需要美术资源，使用简单几何图形加一个字表示。
- 已确认地图长边定义为 100 个逻辑单位，速度数值表示每秒移动的逻辑单位数。
- 已创建 `plan/` 文件夹用于规划当前正在推进的版本计划。
- 当前 V0.1 开发计划文档为 `plan/V0.1基础验证版本开发计划.md`，状态为已完成。
- 当前 V0.2 开发计划文档为 `plan/V0.2任务进化与设计师文档开发计划.md`。
- 后续版本路线图为 `plan/后续版本路线图.md`。
- 当前 V0.3 开发计划文档为 `plan/V0.3可玩性验证与调试闭环开发计划.md`。
- 当前 V0.4 开发计划文档为 `plan/V0.4联机对局开发计划.md`。
- 当前设计师文档入口为 `开发文档/设计/设计师文档.md`。
- 已确认卡牌数值权威数据源从 `游戏工程/scripts/v01/card_catalog.gd` 迁移到 `游戏工程/data/cards.json`；`card_catalog.gd` 现在只负责读取 JSON 并转换 Godot 运行时字段。
- 已新增设计师数值调校工具 v0：`tools/balance_studio/`。该工具通过本地 Python 标准库服务读取/保存 `cards.json`，并在保存时同步刷新 `开发文档/设计/设计师文档.md`。
- `开发文档/设计/设计师文档.md` 当前是设计师可读快照，不再是权威数值源；协作者不要再把 `card_catalog.gd` 当作数值表编辑。
- `游戏工程/` 已新增 V0.1 基础验证原型，主场景为 `res://scenes/v01/V01Game.tscn`。
- 已完成一项不纳入当前路线图的玩家体验界面改造：启动主界面只保留单机、联机、图鉴、牌组四个入口，并展示当前已保存牌组；图鉴为上方详情、下方单位仓库，目前每页 8 个条目并展示可携带卡、禁用卡与衍生单位；禁用主卡右上角显示红橙色“禁用”，衍生物显示蓝灰色“衍生”。牌组为上方 6 卡编辑区、下方可携带卡仓库，同样每页 8 张，保存到 `user://selected_deck.json` 后供单机与联机共用；战斗中可用 Q/W/E/A/S/D 依次选择 6 张卡。
- 已将《百骸公国_完整卡牌设计》中 8 张主卡与 3 个侍童衍生单位实装；当前 7 张百骸主卡可携带，见习骑士征召因强度过高禁用但仍保留在图鉴。新单位当前没有美术资源，用圆形、方形、三角、菱形和六边形几何占位。召唤、阵亡回费/爆炸/火区、侍童死亡成长、实时光环、伤害转移、延迟召唤、阵亡回费区域和对应全局任务已进入定点模拟。
- Bot 的随机与固定牌组入口都会过滤 `ai_deckable=false` 的卡；当前百骸一心已标记为仅玩家可携带，Bot 不会带入对局。裂魂矢、星陨及星陨进化后的星散会从施法方基地发射确定性弹道，在 60Hz 模拟的第 60 tick（1 秒）抵达目标点后结算；裂天矢仍为即时路径法术。弹道状态参与联机 checksum。
- 百骸一心与万骸一心共享基础卡 `one_bone_heart` 的 10 秒（600 tick）内置出牌冷却；每个独立施放区域累计最多实际回复 10 点法力，满费溢出不消耗额度。战斗卡槽显示冷却剩余时间，冷却状态与区域累计回费参与联机 checksum。
- 所有单位普攻在原有即时结算时生成短促攻击轨迹与命中闪光，按本机实际控制阵营显示为我方蓝青色、敌方红橙色；联机客机控制 `bot` 时也会正确反转视角。AOE 普攻同时显示实际落点范围，额外多目标各有独立反馈；这些事件只存在于非同步表现队列，不改变伤害时序或 checksum。方旗骑士、水脉骑士及其进化形态会常驻显示浅色的“附近己方侍童阵亡”实际触发范围圈，半径直接读取 `squire_death_buff.radius`（当前分别为 13、18）。
- 当前可玩原型已覆盖：15 张可携带卡中选 6、1 张禁用主卡保留在图鉴、对局中 6 张常驻卡、3 个不可携带衍生单位、费用回复与上限、玩家部署、脚本 Bot 出牌、单位推进/索敌/攻击、基地 300 血与摧毁胜负、基地 200/100 血阈值清屏、中央时钟、调试信息、结算统计、任务/进化 UI 与百骸公国机制。
- V0.1 已完成，人工验收没有发现问题。
- 当前调参：所有生物单位表现与碰撞半径扩大到原型初始值的 2 倍；费用自然回复为每秒 0.5 点，费用上限仍为 10 点。
- 已启动一项不纳入当前版本开发时间线的 AI 单位美术实验：目标是为 6 个基础生物单位与 6 个对应进化形态制作正面/背面图片，并替换几何占位表现；该工作只允许改变表现层，不得改变现有单位碰撞半径、战斗数值或玩法逻辑。已确认风格为明亮卡通、比例夸张、轮廓清晰、小尺寸可读，主体为奥术并可按单位主题混入机械/黑客等元素；我方单位永远显示背面，敌方单位永远显示正面；现有蓝/红阵营识别环继续单独绘制，不把阵营色烘焙进图片。原单位基准盾卫样张 2C 后续被手机端反馈覆盖：当前角色美术在手机上过于模糊，后续单位必须大幅缩减细节，转向低细节、大块面、强剪影、少内线的移动端可读版本。第一张精致奥术机械 SD 方向仍保留给未来地图、边框与 UI 参考；后续剪影增强应保持静态站姿，避免看起来在走动或冲刺，并避免通过碎切面、小纹路和小装饰增加复杂度。
- AI 单位美术首批资源已完成并接入表现层：12 个单位形态各有正面/背面透明 PNG，正式资源位于 `游戏工程/assets/units/`，源图位于 `游戏工程/assets/units/source_sheets/`，生产流程见 `开发文档/美术/AI单位美术生产流程.md`。这些首批资源现在只视为 trial 版本，不再视为最终单位美术基准；已新增 low 与 ultra 两档盾卫低细节候选，源图位于 `游戏工程/assets/units/source_sheets/candidates/`，透明正背面位于 `游戏工程/assets/units/candidates/`，小尺寸 QA 预览为 `开发文档/美术/AI单位美术低细节盾卫候选预览.png`。当前仅将 low 候选临时实装到正式盾卫 `shield_pair` 正面/背面/源图，原盾卫 2C 资源已备份到 candidates 目录；其它 11 个正式单位资源未替换。`游戏工程/scripts/v01/v01_game.gd` 会按 `art_id + front/back` 动态加载，缺图时 fallback 到旧几何占位图形；卡牌数据文件不承担美术路径配置。
- V0.2 至少需要创建并维护面向设计师的卡牌数值文档，记录卡牌速度、血量、攻击力等信息，并以设计师可读、可编辑为目标；该文档是说明文档，可以当成“有注释的 JSON 数据”。
- 如果发现设计师文档变化了，应向设计师询问是否需要同步到工程数据或其他文档。
- V0.2 需要完成任务与进化系统；已确认 8 张初版卡都会有任务和进化，每张卡默认 1 个任务，暂时不做局外任务配置，先做 1 层进化，任务进度仅在局内生效，任务完成后自动且不可避地进化到对应下一形态。
- 已确认 8 张初版卡的 V0.2 任务与进化路线：星屑群→星屑漂流，盾卫→坚盾护卫，旋刃兵→轮刃兵，连弩手→漫游弓手，秘火手→投火手，奥术巨像→奥能巨像，裂魂矢→裂天矢，星陨→星散。
- 当前 V0.2 原型已实现：任务状态初始化、局内进度追踪、任务完成自动进化、进化后卡牌定义、底部卡牌栏进度显示、事件日志、结算统计、进化后单位/法术行为。
- 当前 V0.2 实现遵循：任务完成后卡牌后续使用变为进化版本；已经在场上打出的单位不受影响。
- 当前 V0.2 微调：裂魂矢对基地伤害为 33；星屑漂流数量为 6；投火手攻击落点 AOE 为固定半径 2，不再采用“若已有 AOE 则 +2”。
- V0.2 已完成，人工验收没有发现问题。
- V0.1/V0.2 工程已从单文件 `scripts/v01/v01_game.gd`（原 1258 行）拆分为按职责分层的模块结构：`config/`、`simulation/`、`presentation/`、`support/` 四层共 7 个模块 + 约 150 行的精简控制器；详见 `开发文档/工程/技术约定.md`。该拆分提前于原 V0.4 计划执行，逻辑逐字搬运、未改数值/时序/行为。
- 已确认 V0.3 不要求做新卡；若设计师想做新卡，可以作为可选内容加入，但需要先明确目标并同步设计师文档。
- 已确认 V0.3 开发者调试工具允许加入，但不应占据主要视觉，只在对局明确需要时开启。
- V0.3 已开始实现：完成了 P1（任务/进化 UI 可读性优化：进度条、进化闪光、卡牌详情面板、结算增强）、P2（Bot 随机选卡、法术目标选择、固定/随机牌组切换）、P3（调试快捷键：加费/减费、强制完成任务、重开、重置种子、切换 Bot 牌组模式、调试面板开关）。
- V0.3 同时修复了两个重构前已存在的 bug：单位过桥卡住（重写路径点逻辑，使用 `>=` 边界和越过边界的路径点）、单位重叠（添加 `_separate_units` 碰撞分离系统）。
- V0.3 碰撞分离引入后的兼容改进已生效：(1) 射程机制改为 `range + self.radius + target.radius`（避免被推开后射程不够）；(2) 战场硬边界 `_clamp_positions_to_map_border` 防止单位被挤出屏幕；(3) 碰撞分离改为"攻击CD中位置锁定"——`attack_timer > 0` 的单位作为固定障碍不被推动，对方被推开全部重叠量，双方都在攻击CD中时保持原位。曾试过"只分离同阵营"方案，用户反馈敌我重叠不好看已撤销。
- 卡牌权威数据源已由 mike 迁移到 `游戏工程/data/cards.json`，`scripts/v01/card_catalog.gd` 现在只是读取适配器；修改卡牌数值应使用 `tools/balance_studio/` 或直接改 JSON，不要再编辑 `card_catalog.gd` 的数值表。
- V0.3 联机前风险记录已整理到 `plan/V0.3可玩性验证与调试闭环开发计划.md`。
- V0.3 调参记录已添加到 `开发文档/设计/设计师文档.md`。
- 已确认 V0.3 Bot 可以改成从当前可用卡池中随机选择 6 张。
- 已确认联机节奏：V0.3 只做联机前风险记录；原定 V0.4 工程拆分已提前完成；V0.4 改为直接实装联机对局（确定性锁步 + 定点数 + Python asyncio 中继 + 房间码 + 互联网经云服务器 + 断线暂停超时判负）；V0.5 释放为联机增强。
- V0.4 实现进度（截至 2026-08-13）：
  • P0 命令层 + 固定 tick：已完成。`scripts/networking/command.gd` 定义 PLAY_CARD_FP / NO_OP / CHECKSUM 三类可序列化命令；`scripts/networking/lockstep_scheduler.gd` 提供命令队列、tick 推进、strict_wait 暂停机制与 checksum 比对；当前 Config 为 60Hz 固定 tick 和 4 tick 输入延迟。
  • P1 定点数 Q*1000：已完成。`game_config.gd` 新增 TICK_RATE/TICK_DT/INPUT_DELAY_TICKS/FP_SCALE/DESYNC_CHECK_INTERVAL 常量；`support/fp_math.gd` 提供 to_fp / dist_sq / move_toward 等纯整数工具；`battle_simulator.gd` 全面改写为定点数（单位 pos_fp/radius_fp/速度/伤害/法力、距离平方比较、碰撞分离整数化并对攻击中单位锁定位置）；`ui_painter.gd` 绘制时转 pos_fp÷1000.0 回到浮点逻辑坐标。
  • P2 锁步 + desync：已完成。`battle_simulator.state_checksum()` 用 FNV-1a 32 位对关键状态混合；当前每 30 tick（0.5 秒）生成 CHECKSUM 命令并与对端比较，不一致则暂停、标记 desynced 并跳结果页。
  • P3 服务器+房间+UI：已完成。新增 `tools/relay_server/relay_server.py`（Python 标准库 asyncio TCP：房间码建房/加入、READY 双向广播、服务器下发 seed+牌组 START、命令中继、RESULT 持久化、PEER_DISCONNECT 超时判负）；新增 `scripts/networking/network_client.gd`（JSON Lines 协议、create/join/ready/send_command/send_result、事件信号）；原始 DECK_SELECT 联机入口后续已由 2026-08-14 玩家体验改造替换为 MAIN_MENU 的“联机”入口与独立 ROOM_SETUP 创建/加入界面，房间协议不变。
  • P4 断线：已完成。服务器 30s 宽限后判断线者负并 OPPONENT_DISCONNECTED_WIN；客户端收到 PEER_DISCONNECT 时 scheduler.paused=true 并显示倒计时横幅，收到 OPPONENT_DISCONNECTED_WIN 跳结果页覆盖胜者。
  • P5 文档同步：已完成（本条目为当前状态更新，配套更新摘要与技术约定同步写入）。
- 当前默认联机参数：TICK=60Hz、INPUT_DELAY=4 ticks（约 67ms）、FP=Q*1000、DESYNC_CHECK_INTERVAL=30 ticks（0.5 秒）。
- 已确认联机角色映射：host 分配 PLAYER 阵营、guest 分配 BOT 阵营；绘制层通过 side 镜像让双方始终显示自己在下半区蓝方。
- 已确认联机 Godot↔Python 协议：TCP JSON Lines（每包以 \n 分隔）。包类型：CREATE_ROOM / JOIN_ROOM / READY / COMMAND / RESULT / ERROR（客户端→服务器），ROOM_CREATED / ROOM_JOINED / PEER_JOINED / PEER_READY / START / COMMAND / RESULT / PEER_DISCONNECT / OPPONENT_DISCONNECTED_WIN / ERROR（服务器→客户端）。详见 `开发文档/工程/技术约定.md`。
- 已确认对局结果持久化：服务器按每次 RESULT 或 30s 断线判负追加写入 `tools/relay_server/match_results.jsonl`，字段含 ts/room_code/winner/seed/host_deck/guest_deck/reporter_side/reason/disconnected。
- V0.2 已通过本机临时冒烟验证：临时 GDScript 脚本直接触发 8 张卡任务并检查关键进化数值，测试通过后已删除临时脚本。
- V0.2 已通过本机 Godot headless 主场景加载检查，命令为 `C:\Tools\godot\godot.cmd --headless --path "游戏工程" --log-file ".godot/godot-cli.log" --quit-after 5`；仍有已知的根证书读取提示。
- 本机临时验证记录：当前设备可通过 `C:\Tools\godot\godot.cmd` 调用 Steam 版 Godot，`--version` 输出 `4.5.2.stable.steam`；这不是项目标准配置，其他协作者不应假设存在该路径。
- 本机曾通过 Godot CLI 做 V0.1 主场景 headless 短跑加载检查，命令见 `开发文档/工程/技术约定.md`。
- 已确认一批基础玩法信息，详见 `开发文档/设计/已确认玩法基础.md`。
- 旧的 `开发文档/项目文档摘要.md` 属于早期占位总文档，已拆分为按功能和读取频率组织的文档。

## 活跃假设

- 现在仓库内的文档主要是协作骨架，不代表完整设定。
- 多名人类设计师可能会补充、修正或废弃早期想法。
- AI 应帮助记录和整理决策，但不能替人类提前定案。
- 旧格式的 `Codex/...：已阅读` 标记只能作为历史阅读痕迹，不能可靠区分具体协作者；新记录从 2026-08-13 起应使用显式签名。
- 后续新增文档应按“是否每次需要读、是否频繁变化、承担什么功能”放入合适位置。
- V0.1 中 8 张卡的数值、Bot 固定节奏策略和地图细节是 trial 原型实现，用于验证闭环，不是最终平衡或长期规则定案。
- 当前 8 张卡的 trial 数值以 `游戏工程/data/cards.json` 为准；工具和 Godot 读取入口都应围绕该 JSON 保持同步。
- 当前 2 倍单位半径来自 V0.1 首轮调参结果；费用回复已在 V0.2 微调为每秒 0.5 点，后续仍可继续按验证反馈调整。
- AI 单位美术当前为 trial revision required：首批 2C 路线资源已接入但手机端可读性不足；当前仅 low 低细节候选被临时实装到盾卫 `shield_pair`，其余单位仍待项目负责人确认后才能批量替换；最终资源仍不得承担碰撞、数值或阵营识别职责。
- V0.2 任务与进化系统的基础边界和 8 张卡具体路线已由项目负责人确认；后续不要在未确认的情况下扩展新任务层数、分支或局外配置。
- 联机是最终产品方向；V0.3 只记录联机前风险；V0.4 已完成首次联机原型落地（锁步+定点数+中继+房间码+断线），定点数手感与 desync 稳定性已做工程实现，仍需双人人工联机手感验收；V0.5 释放为联机增强（重连/弱网/匹配等）。
- 活跃实现假设：Godot StreamPeerTCP 与 Python asyncio 之间以 UTF-8 JSON Lines 行传输即可满足联机原型需求；Q*1000 定点精度足够；每 30 tick 一次 FNV-1a 校验可以早期发现常见 desync（随机源、浮点、单位迭代顺序差异）；host→PLAYER / guest→BOT 的阵营映射可保持双方"我在下半区蓝方"的视觉惯例。

## 开放问题

- 是否已有仓库外的人类设定稿、表格、原型或技术选择需要迁入？
- 当决策记录增多后，是否需要按玩法、叙事、工程等方向拆分为多份决策日志？
- 初版 8 种卡的 trial 数值、费用和行为规则是否需要调整或确认？
- 未来卡池扩充的节奏、解锁方式和总量规划是什么？
- 百骸公国设计稿使用“低/中/高”定性数值，当前 JSON 中的生命、攻击、攻速、移速、射程、爆炸与火区伤害均为工程 trial 映射；人工试玩后需要确认定性档位的统一数值表与平衡结论。
- 任务/进化 UI 的最终产品形态是否沿用底部卡牌栏，还是改为独立侧栏、弹出详情或复合显示？
- 定点数精度（Q*1000）与移动量化手感已以当前工程实现并作为默认假设，但仍需双人联机人工验收确认；若出现视觉抖动或明显错判，是否考虑提高到 Q*10000 或引入软绘制插值？
- 锁步默认参数（tick 60Hz、输入延迟 4 tick、desync 校验间隔 30 tick）已写入 Config；双人实测反馈后是否需要拉长/缩短 input delay 或改变校验频率？
- desync 发生时当前采取"暂停并跳结果页提示重开"；待明确需要记录的 desync 诊断信息（tick、checksum、双方单位快照）以及是否通过服务器自动保存 desync 报告。
- 联机中继服务器部署：当前约定为 Python asyncio + 裸 IP + PORT + 防火墙放行；互联网云服务器的具体规格/成本/位置、是否需要 systemd 守护脚本、是否需要域名/证书/WSS 都仍待项目负责人或运维确认。
- 弱网处理：当前只有 strict_wait 暂停（缺命令就卡），V0.5 是否需要加入本地回退预测（仅视觉）、抖动缓冲区、包冗余（重复发送最近命令）等？
- 房间码仅 4 位，短期可容纳空间约 32^4 ≈ 1M；若同时在线房间增长是否需要改为 6 位或引入短租回收？
- Godot 客户端连接服务器地址目前写死在 `network_client.default_host`（127.0.0.1:8765），是否需要改为启动前 UI 输入服务器地址或读配置文件？
- V0.3 如果设计师决定试做新卡，新卡数量、定位、任务和进化是否需要同步纳入设计师文档？
- 进化后的视觉表现是否需要额外美术、音效或更明显的特效提示？
- 临时实装到 `shield_pair` 的 low 低细节盾卫在 Godot 编辑器或运行窗口、手机竖屏实际战场中是否清晰？若通过，再决定批量替换其它 11 个单位，或改用/继续迭代 ultra 方向；检查重点包括遮挡、单位尺寸、生命条位置、卡牌图标可读性和阵营环识别。
- 当前数值工具 v0 已把 JSON 保存与设计师文档快照同步工具化；后续是否需要把工具保存进一步接入 Godot 热重载、对局复盘导入或调参记录模板，仍待验证。
- V0.1 脚本 Bot 当前为固定节奏出牌策略，是否需要调整难度或加入防守/分路偏好？
- Godot 工程的导出目标、自动化测试方式、跨协作者统一 CLI 验证入口是什么？
- 当前设备的 Steam 版 Godot CLI 曾出现应用程序错误弹窗；是否需要改用官方独立 Standard 版作为本机命令行验证入口？

## 近期记录

### 2026-08-14

- 完成百骸公国卡牌与仓库分页：
  - 权威卡牌数据扩充为 16 张主卡（当前 15 张可携带、1 张禁用）+ 3 个不可携带侍童；图鉴共 3 页，牌组可选仓库共 2 页，每页 8 个条目。
  - 实装胆怯侍从、方旗骑士、大十字骑士、管网骑士、水脉骑士、见习骑士征召、小小骑士团、百骸一心及各自任务/进化；实装书卷、持剑、启明三种侍童。
  - 数值是从定性档位转换的 trial；新单位无美术资源，图鉴和战场使用几何形状回退。
  - 新增机制状态与任务进度已纳入锁步 checksum；项目数值工具可验证衍生单位并同步生成设计师快照。
  - Godot 4.5.2 主场景 headless 加载通过；机制矩阵、checksum 双实例一致性与 3600 tick 双方百骸长局冒烟通过；720×1280 图鉴第 2/3 页和牌组第 2 页已做图形 QA。

- 完成版本线外玩家体验界面改造：
  - 启动后进入 MAIN_MENU，只显示单机、联机、图鉴、牌组四个入口，并在下方展示当前已保存牌组。
  - 新增 COMPENDIUM：上方显示当前卡牌数值、任务和进化说明，下方显示全部 8 张卡的单位仓库。
  - 新增 DECK_BUILDER：上方编辑 6 个牌组位置，下方从单位仓库增删卡牌；只有恰好 6 张时可保存，未保存返回会恢复原牌组。
  - 当前牌组保存在 `user://selected_deck.json`，读取时校验不存在/重复卡牌；存档无效时回退到卡池前 6 张。单机与联机准备都读取该牌组。
  - 战斗卡槽新增 Q/W/E/A/S/D 标签与快捷选择；原调试键 D/S 调整为 B/Y，避免覆盖玩家输入。
  - 同步修正联机 guest 的本机卡牌栏、费用与任务详情读取阵营（painter.controlled_side = my_game_side + selected_card_ids = my_deck.duplicate()），确保快捷选卡指向自己的 6 张牌。（注：本次与结算异常修复合并时做了 git 冲突解决。）
  - Godot 4.5.2 headless 启动与玩家体验逻辑冒烟通过；720×1280 图形渲染截图已检查主界面、图鉴和牌组界面，无溢出。

- 修复 V0.4 联机模式下<1秒极速结算并显示错位胜者的连锁BUG：
  - 根因链路：FNV未截断→假性desync→running=false走上报流程→match_winner空→兜底为"draw"→_on_result_received两端i_won=false→各自设peer_game_side作为override_winner→房主"Bot胜利"、客机"我方胜利"。
  - 修复5处代码：(1) battle_simulator._fnv_mix + state_checksum 每步严格截断32位；(2) MapMath.side_name 空字符串返回"平局"；(3) _on_result_received 对 winner=draw 单独分支设 override_winner="" 显示平局；(4) _process running=false 分支加 scheduler.desynced 判断 + 仅 report_side≠"" 才上报，避免 desync 异常污染 match_results.jsonl；(5) _start_online_battle 开局重置 painter.override_winner 和 bot_brain。
  - 与客机端阵营同步改动（cbea5179）做三方合并：保留了 NO_OP 预发、状态重置、selected_card_ids/controlled_side 同步三点全部功能，无语义冲突。
  - GetDiagnostics 零错误，Python relay_server 语法检查通过。

### 2026-08-13

- 立项 V0.4 联机对局版本：经与项目负责人确认，V0.4 直接实装联机（原 V0.4 工程拆分已提前完成）。联机方案定为确定性锁步 + 定点数整数运算 + Python asyncio 中继服务器（中继/房间码/仅持久化对局结果）+ 互联网经公网云服务器 + 房间码无账号 + 断线暂停超时判负。计划草案写入 `plan/V0.4联机对局开发计划.md`，分 P0–P5 六阶段。
- V0.4 实现落地（P0–P5）：
  * 命令层与固定 tick：新增 `scripts/networking/command.gd` 与 `lockstep_scheduler.gd`，控制器推进改为 20Hz。
  * 定点数 Q*1000：新增 `support/fp_math.gd`；`battle_simulator.gd` 全面改为 int 定点；`ui_painter.gd` 绘制时 ÷1000.0 还原。
  * 锁步 + desync：`state_checksum` 用 FNV-1a，每 10 tick 比对；不匹配即暂停并跳结果。
  * 中继服务器：`tools/relay_server/relay_server.py`（asyncio TCP，房间码/START/READY 广播/命令中继/RESULT 持久化/30s 超时判负）。
  * Godot 网络层：`scripts/networking/network_client.gd`（JSON Lines 协议、事件信号、TCP 轮询）。
  * 房间 UI：最初在 DECK_SELECT 底部新增"创建/加入"联机按钮；2026-08-14 玩家体验改造后入口移到 MAIN_MENU 的“联机”按钮，ROOM_SETUP/ROOM_WAIT、断线倒计时与结果页覆盖胜者逻辑继续沿用。
  * 文档同步：更新摘要、当前状态、技术约定三份协作/工程文档同步写入 V0.4 联机约定与协议细节。
- 记录项目负责人确认盾卫样张 2C 可作为当前单位生产基准，暂时停止继续迭代。
- 完成 6 个基础单位与 6 个进化形态的正面/背面 AI 单位资源生产、透明 PNG 后处理、Godot 表现层接入和流程文档。
- 接入保持表现层边界：不修改碰撞半径、战斗数值、费用、任务/进化逻辑或法术表现；缺少图片时 fallback 到旧几何占位。
- 记录项目负责人反馈首批角色美术在手机上过于模糊，旧 2C 单位基准被后续低细节移动端方向覆盖；已生成 low/ultra 两档盾卫低细节候选和 32/45px 小尺寸 QA 预览，正式 12 个单位 PNG 暂未覆盖。
- 按项目负责人要求暂不批量替换，仅将 low 低细节候选临时实装到正式盾卫 `shield_pair` 资源上；原 2C 盾卫备份到 candidates 目录，其它 11 个单位保持不变。
- 根据项目负责人确认，开始制作设计师数值平衡与调校工具；已将卡牌权威数据源迁移到 `游戏工程/data/cards.json`，新增 `tools/balance_studio/` 网页 v0，并明确 `设计师文档.md` 为工具同步快照。

- 记录项目负责人确认协作记录需要区分创建者与协作者；随后修正确认：协作者签名是本地/会话信息，不得在 GitHub 共享文档中写死为“本机签名”。
- 更新协作记录格式：
- 记录项目负责人确认协作记录需要区分创建者与协作者；随后修正确认：协作者签名是本地/会话信息，不得在 GitHub 共享文档中写死为“本机签名”。
- 更新协作记录格式：
  - `开发文档/协作/更新摘要.md` 新增 `创建者：签名/任务标识` 字段。
  - 更新摘要的阅读标记改为显式写明 `协作者：签名` 与任务标识。
  - `开发文档/协作/决策记录.md` 新增“创建者”字段；旧条目不事后猜测创建者。
  - `AGENTS.md` 写入创建者/协作者区分规则，并要求 AI 在未知当前签名时先询问。

### 2026-08-12

- 新增较大更新摘要流程：
  - `开发文档/协作/更新摘要.md` 用于倒序记录较大更新的简短说明。
  - 协作者看到未读更新后，应在对应条目下用简短标记说明“已阅读”。
- 规划基础验证版本前读取现有文档与 Godot 工程状态。
- 发现 `开发文档/设计/核心哲学.md` 中关于 Godot 版本未确认的旧表述与其他文档及工程文件冲突；经项目负责人确认后已清理该旧表述。
- 根据项目负责人补充，记录 V0.1 基础验证范围：本地玩家对脚本 Bot、不做联网、摧毁基地结束、中央时钟辅助观察、任务/进化只预留接口和 UI 位置。
- 记录 V0.1 单位表现规则：不需要美术资源，使用简单几何图形加一个字表示。
- 记录统一尺度规则：地图长边为 100 个逻辑单位，速度数值表示每秒移动的逻辑单位数。
- 记录工程确认信息：Godot 脚本语言使用 GDScript。
- 新建 `plan/` 文件夹，并将 V0.1 基础验证版本开发计划整理为 `plan/V0.1基础验证版本开发计划.md`，用于后续对话调用。
- 落地 V0.1 Godot 基础验证原型：
  - 主场景：`游戏工程/scenes/v01/V01Game.tscn`。
  - 代码：`游戏工程/scripts/v01/v01_game.gd`。
  - 卡牌数据：`游戏工程/scripts/v01/card_catalog.gd`。
  - 已实现 8 选 6、费用部署、脚本 Bot、单位战斗、基地清屏、中央时钟、结算统计、任务/进化预留。
  - 命令行环境未找到 `godot`，仍需用 Godot 编辑器运行做最终验收。
- 验证 Steam 版 Godot CLI：
  - `C:\Tools\godot\godot.cmd --version` 已输出 `4.5.2.stable.steam`。
  - 带 `--log-file ".godot/godot-cli.log"` 的 headless 主场景短跑加载检查已通过，退出码为 0。
  - 上述路径只是当前设备配置，不是项目标准配置；协作者可使用自己的 Godot 4.5 stable 安装路径。
  - 当前设备随后出现过 `godot.windows.opt.tools.64.exe` 应用程序错误弹窗，疑似 Steam 版 CLI 或日志路径相关环境问题，仍需观察。
  - 重新确认没有手动打开的 Godot 进程后再次复测，版本检查与 headless 短跑加载检查均通过，未复现应用程序错误弹窗，检查后无残留 Godot 进程。
  - 基础验证已通过；命令行检查仅覆盖加载和短跑，不替代后续调参后的可玩性观察。
- 记录基础验证通过后的首轮调参：
  - 所有生物单位表现与碰撞半径扩大到原型初始值的 2 倍。
  - 费用自然回复从每秒 1 点改为每秒 2 点，费用上限仍为 10 点。
  - 调参后通过 `C:\Tools\godot\godot.cmd --headless --path "游戏工程" --log-file ".godot/godot-cli.log" --quit-after 5` 短跑加载检查，退出码为 0；仍有已知的根证书读取提示。
- 记录项目负责人确认：V0.1 已完成，人工验收没有发现问题。
- 新建 `plan/V0.2任务进化与设计师文档开发计划.md`，作为当前正在推进的版本计划。
- 新建 `开发文档/设计/设计师文档.md`，作为面向设计师的卡牌数值说明入口。
- 记录 V0.2 目标：创建并维护设计师文档，完成任务与进化系统；任务与进化基础边界已确认，当时 8 张卡各自的任务条件和进化效果仍需人类确认，现已由项目负责人补充确认并实现。
- 记录 V0.2 基础决策：
  - 设计师文档是说明文档，可以当成“有注释的 JSON 数据”；设计师有阅读代码的能力，但需要完善注释系统定位可编辑内容。
  - 如果发现设计师文档变化，应向设计师询问是否需要同步。
  - 8 张初版卡都会有任务和进化。
  - 每张卡默认 1 个任务，V0.2 暂时不做局外任务配置。
  - V0.2 先进化 1 层，任务进度仅在局内生效，任务完成后自动且不可避地进化到对应下一形态。
- 记录并实现项目负责人确认的 8 张卡任务与进化路线：
  - 星屑群→星屑漂流，盾卫→坚盾护卫，旋刃兵→轮刃兵，连弩手→漫游弓手，秘火手→投火手，奥术巨像→奥能巨像，裂魂矢→裂天矢，星陨→星散。
  - 工程实现集中在 `游戏工程/scripts/v01/card_catalog.gd` 与 `游戏工程/scripts/v01/v01_game.gd`。
  - 对局 UI 已在底部卡牌栏显示任务进度/已进化状态，结算统计显示双方任务完成和进化数量。
  - 本机临时 GDScript 冒烟脚本验证 8 张卡关键进化数值通过，脚本已删除；随后 Godot headless 主场景加载检查通过，仍有已知根证书读取提示。
- 根据项目负责人反馈完成 V0.2 数值微调：
  - 费用自然回复改为每秒 0.5 点，费用上限仍为 10 点。
  - 裂魂矢对基地伤害改为 33。
  - 星屑漂流数量改为 6。
  - 投火手攻击落点 AOE 改为固定半径 2，不再使用“若已有 AOE 则 +2”的规则。
- 记录项目负责人确认：V0.2 已完成，人类验收没有发现问题。
- 新建 `plan/后续版本路线图.md`，规划 V0.3 到 V0.6 的建议方向。
- 新建 `plan/V0.3可玩性验证与调试闭环开发计划.md`，建议下一版本聚焦任务/进化可读性、Bot 覆盖、调试工具和设计师调参闭环；后续已由项目负责人总体确认并调整为当前 V0.3 计划。
- 记录项目负责人确认：奥术前线是联机游戏，最终需要实现联机对战。
- 调整后续路线建议：V0.3 不直接实现联机，但记录联机前风险；V0.4 做联机前工程结构整理；V0.5 做最小联机对局原型；V0.6 再进入移动端联机体验与构建验证。
- 记录项目负责人总体确认后续路线，并补充 V0.3 调整：
  - V0.3 不要求做新卡，但设计师如果想做也可以。
  - 开发者调试工具是好的，但不应占据主要视觉，只在对局明确需要时开启。
  - Bot 可以改成随机选 6。
  - 确认 V0.3 只做联机前风险记录，V0.4 再做工程拆分，V0.5 再做最小联机对局原型。

### 2026-08-11

- 建立根目录工作流入口 `AGENTS.md`，用于告诉所有 AI 每次应读哪些文件、不必读哪些文件。
- 将原本集中在占位摘要中的内容拆分为：
  - `开发文档/协作/当前状态.md`
  - `开发文档/协作/决策记录.md`
  - `开发文档/设计/核心哲学.md`
  - `开发文档/工程/技术约定.md`
- 记录前期人类确认信息：
  - 项目类型参考《皇室战争》/《随从大师》。
  - 主要载体为 Godot。
  - 主要目标用户为手机竖屏。
  - 初版卡池、局内成长和局内基础机制已整理到 `开发文档/设计/已确认玩法基础.md`。
- 记录补充确认信息：
  - 双方各有一个基地，每个基地 300 血。
  - 单局 6 种卡是在开局前从当前可用卡池中配置；未来卡池会扩充到不止 8 种，但单局仍只能带 6 种。
- 记录工程确认信息：
  - 使用 Godot 4.5 stable。
  - `游戏工程/` 已创建 Godot 空项目，当前 `project.godot` 包含 `4.5`、`Mobile` 和 `mobile` 渲染配置。

## 并行协作提醒

- 开始工作前检查 `git status --short`。
- 不要回退、覆盖或重写他人未提交改动。
- 若同一系统出现两种互相冲突的说法，记录冲突并询问人类设计师。
- 若新增大段文档，应先判断它属于“高频状态”“人类决策”“核心设计”“工程约定”还是具体功能文档。
